%nav.navbar.navbar-default
  .container
    .navbar-header
      = link_to root_path do
        = image_tag 'logocmx@2x.png', width: '220px'
    #navbar.collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-right
        %li.dropdown
          %a.dropdown-toggle{ href: "#", 'data-toggle'=> "dropdown", role: "button", 'aria-haspopup'=> "true", 'aria-expanded'=> "false" }
            Participa
            %span.caret
          %ul.dropdown-menu{ role: 'menu' }
            %li
              %a{ href: "#"} Retos cívicos
        %li
          %a{ href: "#" } Acerca
        - if user_signed_in?
          %li.dropdown
            %a.dropdown-toggle{ href: "#", 'data-toggle'=> "dropdown", role: "button", 'aria-haspopup'=> "true", 'aria-expanded'=> "false" }
              = current_user
              %span.caret
            %ul.dropdown-menu{ role: 'menu' }
              %li
                = link_to t('header.user.panel'), panel_entries_path
              %li
                = link_to t('header.user.profile'), edit_member_path(current_user.userable)
              %li
                = link_to t('header.user.log_out'), destroy_user_session_path, method: :delete
        - else
          %li
            = link_to t('header.user.log_in'), signup_path
