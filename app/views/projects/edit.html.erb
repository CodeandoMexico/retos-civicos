<section id="pitch" class="pitch row">
  <header>
    <h1><%= t('.header') %></h1>
  </header>
</section>

<%= render 'form' %>

<%= link_to 'Cancelar el proyecto', cancel_project_path(@project), method: :put %> 